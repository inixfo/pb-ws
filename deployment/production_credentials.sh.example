#!/bin/bash

# IMPORTANT: Copy this file to production_credentials.sh and add your actual credentials
# Make sure production_credentials.sh is in .gitignore and never committed to the repository

# Google OAuth Credentials
export GOOGLE_CLIENT_ID="your_google_client_id_here"
export GOOGLE_CLIENT_SECRET="your_google_client_secret_here"

# Instructions for use:
# 1. Copy this file: cp production_credentials.sh.example production_credentials.sh
# 2. Add your actual credentials to production_credentials.sh
# 3. Source the file before deploying: source ./production_credentials.sh
# 4. Run your deployment scripts 